---
# Роль: fail2ban
# Задачи: Установка и запуск fail2ban

- name: Установить fail2ban
  ansible.builtin.apt:
    name: fail2ban
    state: present

- name: Запустить и включить fail2ban
  ansible.builtin.service:
    name: fail2ban
    state: started
    enabled: yes
